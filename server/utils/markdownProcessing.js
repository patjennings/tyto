// fonction qui prend le markdown et le renvoie formatté pour les niveaux de zoom
var sliceMarkdown = function(data){
    return(data+" formaté");
}
var markdownToHtml = function(data){
    // avec marked > https://marked.js.org/#/README.md#README.md
}
var HtmlToMarkdown = function(data){
    // avec marked > https://marked.js.org/#/README.md#README.md
}


module.exports = {
    sliceMarkdown: sliceMarkdown,
    markdownToHtml: markdownToHtml,
    HtmlToMarkdown: HtmlToMarkdown
}
